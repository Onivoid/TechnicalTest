/**
 * Analyse une chaîne de caractères et retourne des statistiques.
 *
 * @param {string} text - Le texte à analyser.
 * @returns {StringAnalysisResult} Un objet contenant les statistiques du texte.
 *
 * @typedef {Object} StringAnalysisResult
 * @property {number} wordCount - Le nombre de mots dans le texte.
 * @property {string} longestWord - Le mot le plus long du texte.
 * @property {number} letterCount - Le nombre total de lettres dans le texte.
 * @property {LetterFrequency} letterFrequency - La fréquence de chaque lettre dans le texte.
 *
 * @typedef {Object.<string, number>} LetterFrequency
 *
 */

// Décommentez et complétez le code suivant :

/*
type LetterFrequency = ;
interface StringAnalysisResult {}
*/

export function stringAnalysis(text: string) {
    // Écrivez votre code ici
    // Pour tester votre code, exécutez la commande `npm run test` dans votre terminal en étant à la racine du dossier Typescript.

    return;
}
